#include<stdio.h>

int input()
    {
        int n,a[1000];
        printf("enter the n value");
        scanf("%d",&n);
        for(i=0;i<n;i++)
        {
             scanf("%d",&a[i]);
        }
        return a[];
     }
int compute()
    {
         int i, sum==0;
         for(i=o;i<n;i++)
         {
             sum=sum+a[i];
         }
         return sum;
    }

int output()
     {
         int i




int main()
    {
         input();
         int a[1000]=
        output(a[]);
    }


